<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naidich.tom.tomsprojectapplication">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.ImagePickerActivity"></activity>
        <activity android:name=".ui.activities.MemeActivity" />
        <activity
            android:name=".ui.activities.SigninActivity"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="adjustNothing|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MainActivity" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:label="@string/title_activity_login"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".ui.activities.TicTacToeActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".ui.activities.CatchEmAllActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".ui.activities.WarGameSelectionActivity"
            android:configChanges="locale|orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".ui.activities.WarGameFightActivity"
            android:screenOrientation="fullSensor" />
        <activity android:name=".ui.activities.ScoreboardActivity" />
    </application>

</manifest>